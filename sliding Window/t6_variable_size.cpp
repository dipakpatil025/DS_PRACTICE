#include <bits/stdc++.h>
using namespace std;

int main()
{
    int arr[] = {4, 1, 1, 1, 2, 3, 5};
    int n = sizeof(arr) / sizeof(arr[0]);

    int k = 5;
    int i = 0;
    int j = n;
    int sum = 0;
    while (j<n)
    {
        sum
    }
    
    return 0;
}